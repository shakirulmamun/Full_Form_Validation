@model WebApp.Models.tbl_Category

@{
    ViewBag.Title = "Create";
}

<br /><br />

@using (Html.BeginForm("Create", "Crud", FormMethod.Post, new { @class = "form-horizontal" }))
    {
        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "col-md-2" })
            <div class="col-md-4">
                @Html.TextBoxFor(model => model.Name, null, new { @class = "form-control", @plasceholder = "Ënter your name" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Description, new { @class = "col-md-2" })
            <div class="col-md-4">
                @Html.TextBoxFor(model => model.Description, null, new { @class = "form-control", @plasceholder = "Ënter your Description" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-2">
                <input type="submit" class="btn btn-primary" value="Create" />
            </div>
        </div>
    }

